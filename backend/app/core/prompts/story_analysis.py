"""
故事分析 Prompt 模板

当前版本：合并为单次 LLM 调用（comprehensive_analysis_gen）
Prompt 拆分为 system（固定指令）+ user（小说正文）
"""

COMPREHENSIVE_ANALYSIS_SYSTEM = """你是短剧改编策划师、视觉资产规划师和视频前置素材整理助手。

## 任务说明
用户将给你一段小说文本、剧情简介或剧本文本，你需要完成以下工作：
1. 提取故事全局信息，包括：概要、题材类型、时代背景、世界观、风格基调、核心人物关系、主要冲突。
2. 为每个重要角色生成稳定、可复用的角色设定信息，用于后续生成人物图。
3. 为每个关键场景生成稳定、可复用的场景设定信息，用于后续生成场景图。
4. 为每个关键道具生成稳定、可复用的道具设定信息，用于后续生成道具图。
5. 生成每集剧本大纲，保证每集具备开场、冲突、推进、收束或悬念。

## 输出原则
- 目标是抽取结构化资产，不是文学化改写。
- 不要编造原文没有出现的关键剧情。
- 如果原文缺少外观、场景或道具细节，只做最小必要补充，优先补充后续文生图和视频生成真正需要的信息。
- 所有人物、场景、道具名称保持前后一致，不要擅自改名或改称呼。
- 人物、场景、道具的输出要便于下游模型直接使用。

## 输出格式
请以 JSON 格式输出，严格遵循以下结构，不要输出任何其他内容，不要用 markdown 代码块、标记或注释：

{{
    "global_setting": {{
        "novel_summary": "故事概要（500-1000字，包含核心情节、人物弧线、关键冲突；如果输入文本很短，概要也应简短，按实际内容写）",
        "world_setting": "世界观描述（时代背景、社会环境、核心规则）",
        "style_tone": "风格基调（悬疑/轻松/热血/治愈等），题材类型",
        "character_relations": "核心人物关系图谱的文字描述，说明各角色之间的关系和核心冲突",
        "suggested_episodes": "根据上述集数或文本长度动态计算出的整数：文本小于300字→1；300至2000字→3到5；大于2000字→按每500至800字1集，上限50",
        "episode_mapping": [
            {{"episode": 1, "chapters": "全文或实际章节范围", "theme": "核心冲突或主题"}}
        ]
    }},
    "character_profiles": [
        {{
            "name": "角色名（中文）",
            "aliases": "别名/外号/英文名（如有）",
            "role": "主角/配角/反派/路人/功能角色",
	        "profile_data": "包括：性别;年龄段;时代基底;国家/朝代;人种;类型基底;脸型五官：直接用于生成角色图片，重点写脸型、颧骨、眉眼、鼻梁、嘴唇、下颌线等辨识度特征;发型发色：重点写长度、层次、走向、是否凌乱、局部异色、发饰情况;肤色;耳饰：如无则写“无”;身材;身高观感;头身比;上身着装;下身着装;鞋子;换装信息：如无则写“无明显换装”;性格特征:核心性格 + 行为习惯 + 口头禅;人物关系。如果原文描写分散在多处，请合并。如果原文描写不足，基于角色设定合理补充。"
            "description": "外观描述：整合上述字段信息，输出一段可直接用于角色图片生成的完整描述，重点突出整体气质、战损或换装状态、配饰与标志性特征。描述必须具体，不要写“长相普通”“穿着朴素”这类模糊词。",
            "image_prompt": "视觉关键词：用于文生图，提炼 8 至 16 个清晰关键词，注意：不同角色的视觉关键词必须有明确区分度，避免混淆。"
        }}
    ],
    "scene_descriptions": [
        {{
            "name": "场景名",
            "place": "地点，国家/朝代，如：伦敦郊外，一座被烧毁的哥特式教堂废墟",
            "time": "时间，如：白天/深夜/清晨",
            "type": "室内/室外",
            "summary": "时代基底，3至5个氛围关键词，如：烟火气、怀旧、温暖",
            "description": "场景的完整格式化描写：直接用于生成场景图，重点写空间布局、环境（地理位置、建筑风格、空间大小）、建筑或室内材质、关键陈设、前后景关系、光线（光源方向、强度）、色温、色调（主色调、对比色）、氛围等。描述必须具体，不要只写“一个房间”“一条街道”，必须保留原文的视觉细节。如果原文描写不足，合理补充。",
            "image_prompt": "视觉关键词：用于文生图，提炼 8 至 16 个清晰关键词，注意：不同场景的视觉关键词必须有明确区分度。"
        }}
    ],
    "prop_descriptions": [
        {{
            "name": "道具名",
            "aliases": "别名/英文名（如有）",
            "importance": "关键/普通",
            "description": "道具的完整格式化描写：直接用于生成道具图，包括外观描述（重点写材质、颜色、形状、尺寸、磨损、刻字、装饰、使用痕迹等）、功能、使用场景、象征意义。必须保留原文的视觉细节。如果原文描写不足，合理补充。",
            "image_prompt": "视觉关键词：用于文生图，提炼 6 至 12 个清晰关键词"
        }}
    ],
    "episode_outlines": [
        {{
            "episode_number": 1,
            "title": "集标题",
            "summary": "本集剧情概要（不超过300字）",
	        "opening": "开场",
            "key_events": ["关键事件1", "关键事件2"],
            "key_characters": ["本集关键角色"],
	        "development ": "推进",
	        "turning_point": "转折",
            "hook": "本集钩子或结尾悬念"
        }}
    ]
}}

## 关键注意事项

### 角色描写
- 外观信息要能支撑角色图稳定生成，优先保证人物辨识度和一致性。
- 如果用户已经提供角色卡字段，例如时代基底、国家/朝代、人种、类型基底、脸型、发型、耳饰、身材、头身比、上身着装、下身着装、鞋子，应尽量保留原始信息，只做轻度整理，不要擅自改写核心设定。
- 人物列表要完整，至少覆盖主要角色和有关键台词的角色。
- 如果原文存在换装或身份变化，要明确拆分不同服装状态。
- 对于耳饰、发饰、鞋子、护甲、战损、污渍、血迹、焦痕等容易影响出图一致性的细节，要明确写出；如原文未提及则写“无”或“未提及”，不要凭空补满。

### 场景描写
- 如果用户已经提供时代基底、国家/朝代、视角要求等场景卡字段，应尽量保留原始信息，只做轻度整理，不要擅自改变时空设定或镜头视角。
- 每个场景要具备清晰的视觉区分度，避免多个场景写得过于相似。
- 场景列表要覆盖关键剧情发生地，而不是泛泛罗列背景地点。
- 如果用户明确要求“俯视图”或“鸟瞰图”，场景描述中要补足顶视角下的空间分布、主体位置、地面材质、光区覆盖范围和整体构图关系，避免仍按平视场景去写。

### 道具描写
- 优先保留对剧情推进有作用的关键道具，不要堆无关小物件。
- 道具描述要服务后续白底图或特写图生成。

### 剧集大纲
- 每集要有明确起承转合。
- 集与集之间要连贯，不要为了凑集数强行拆剧情。
- 短文本允许只输出 1 集，长文本按情节复杂度合理拆分。
- 剧集大纲里的集数episode_number应该与episode_mapping中的集数一一对应，连续递增，不能跳跃或缺失。

### 集数动态确定
- 若输入文本已经明确集数要求，则按要求输出。
- 若无明确集数要求，则根据文本长度和情节复杂度决定：
  - 文本字数 < 300 字：建议 1 至 2 集，通常 1 集即可
  - 文本字数 300 至 2000 字：建议 3 至 5 集
  - 文本字数 > 2000 字：通常每 500 至 800 字 1 集，上限 50 集
- 第x集要从1开始连续递增，不能跳跃。
"""


COMPREHENSIVE_ANALYSIS_USER = """请仔细阅读以下小说文本，按照系统指令完成分析。

## 小说文本
{novel_text}"""


COMPREHENSIVE_ANALYSIS_PROMPT = COMPREHENSIVE_ANALYSIS_SYSTEM + "\n\n" + COMPREHENSIVE_ANALYSIS_USER


INCREMENTAL_ADJUSTMENT_PROMPT = """你是一个短剧编剧助手。以下是一个短剧项目的当前状态，请根据用户的调整要求修改相应内容。

## 项目当前状态
{context_snapshot}

## 调整历史
{adjustment_history}

## 用户本次调整要求
{user_instruction}

## 输出要求
请以 JSON 格式输出完整的修改后内容（包含修改和未修改的部分）：

{{
    "global_setting": {{...}},
    "character_profiles": [...],
    "scene_descriptions": [...],
    "prop_descriptions": [...],
    "episode_outlines": [...],
    "change_summary": "本次调整的变更摘要"
}}

## 注意事项
- 只输出被修改的部分，未修改的部分保持不变
- 如果修改影响其他子项的一致性（如修改人物特征影响了分镜中的角色行为），需同时输出受影响的子项
- 确保修改后的内容与整体故事保持一致
- 只输出 JSON，不要输出任何其他内容，禁止任何markdown标记或注释
"""


# ============ 旧版 Prompt（注释保留，合并前的多 Layer 方案） ============
#
# COMPREHENSIVE_EXTRACTION_PROMPT = """你是一个专业的短剧编剧。请仔细阅读以下小说文本，进行详尽的内容提取。
# 你的提取结果将作为后续所有分析的原始素材，因此必须尽可能保留原文中的视觉细节。
#
# ## 小说文本
# {novel_text}
#
# ## 输出要求
# 请以 JSON 格式输出以下内容：
#
# {{
#     "novel_summary": "故事概要（500-1000字，包含核心情节、人物弧线、关键冲突）",
#     "world_setting": "世界观描述（时代背景、社会环境、核心规则）",
#     "style_tone": "风格基调（悬疑/轻松/热血/治愈等）",
#     "character_relations": "人物关系图谱的文字描述，说明各角色之间的关系和冲突",
#     "character_details": [
#         {{
#             "name": "角色名",
#             "role": "主角/配角/反派/路人",
#             "raw_appearance": "从原文中提取的外貌描写原文或转述，要保留所有视觉细节：发型、肤色、体型、穿着、标志性特征（伤疤、痣、饰品等）。不要概括，要具体。",
#             "raw_personality": "从原文中提取的性格描写，包含核心性格、行为模式、典型反应",
#             "key_behaviors": ["这个角色做了什么关键的事", "另一个关键行为"],
#             "key_dialogues": ["角色的典型台词或口头禅（原文）"],
#             "relationships": "与哪些角色是什么关系，关键互动",
#             "arc": "人物弧线：从...状态到...状态的转变"
#         }}
#     ],
#     "scene_details": [
#         {{
#             "name": "场景名",
#             "type": "室内/室外",
#             "raw_description": "从原文中提取的环境描写原文或转述，保留所有感官细节：视觉、听觉、嗅觉、触觉。不要概括，要具体。",
#             "spatial_layout": "空间布局描述（前后左右有什么、远近关系）",
#             "lighting": "光线描述（自然光/人工光、明暗、色温）",
#             "time_of_day": "白天/夜晚/黄昏/清晨",
#             "weather": "天气（如有描写）",
#             "atmosphere": "氛围关键词（3-5个词）",
#             "fixed_objects": ["场景中固定存在的物品"]
#         }}
#     ],
#     "prop_details": [
#         {{
#             "name": "道具名",
#             "importance": "关键/普通",
#             "raw_description": "从原文中提取的道具外观描写，保留所有视觉细节：材质、颜色、形状、大小、磨损、刻字、装饰等",
#             "usage_context": "在什么场景中由谁使用，用于做什么",
#             "symbolic_meaning": "象征意义（如有）"
#         }}
#     ],
#     "chapter_summaries": [
#         {{"chapter_number": 1, "title": "章节标题", "summary": "本章内容摘要（100-200字）"}}
#     ],
#     "suggested_episodes": 10,
#     "episode_mapping": [
#         {{"episode": 1, "chapters": "第1-3章", "theme": "开端"}}
#     ]
# }}
#
# ## 关键注意事项
# - character_details 中的 raw_appearance 是最核心的字段，将直接用于生成角色图片，必须详尽
# - scene_details 中的 raw_description 同样核心，将直接用于生成场景图片
# - prop_details 中的 raw_description 必须包含足够的视觉细节
# - 如果原文中对某个角色的外貌描写分散在多处，请合并到 raw_appearance 中
# - 如果原文中对某个角色的外貌描写很少，请在 raw_appearance 中标注"原文描写不足"，并基于角色设定合理补充
# - 人物列表要完整，包括所有有台词的角色
# - 场景列表要覆盖所有关键场景
# - 集数建议基于故事体量，通常每集3-5分钟
# """
#
# CHARACTER_GEN_PROMPT = """你是一个短剧角色设计师。以下是从小说中提取的角色原始信息，请将其格式化为标准的角色描写，
# 用于指导角色图片生成。
#
# ## 角色原始信息
# {character_details}
#
# ## 人物关系
# {character_relations}
#
# ## 输出要求
# 请以 JSON 数组格式输出每个角色的标准描写：
#
# ```json
# [
#   {{
#     "name": "角色名",
#     "role": "主角/配角/反派/路人",
#     "description": "角色的完整格式化描写，包含外貌特征、性格特征、人物弧线、关键关系等，适合用于图片生成的详细描述文本",
#     "image_prompt": "中文逗号分隔的视觉关键词，如：年轻女性，黑色微卷长发，左耳垂有小痣，蓝色工装，袖口挽起，黝黑皮肤"
#   }}
# ]
# ```
#
# 其中 `description` 是完整的角色描写文本（外貌+性格+弧线+关系），`image_prompt` 是浓缩的视觉关键词，直接用于图片模型。
#
# ## 注意事项
# - description 和 image_prompt 必须基于上方的原始信息，不要编造原文没有的细节
# - 如果原始信息标注了"原文描写不足"，请在保留已有细节的基础上合理补充，并标注[补充]标记
# - 每个角色的视觉特征要独特，避免角色之间混淆
# - 穿着描述要考虑角色在不同场景的换装（如有）
# - 只输出 JSON 数组，不要输出任何其他内容
# """
#
# SCENE_GEN_PROMPT = """你是一个短剧场景设计师。以下是从小说中提取的场景原始信息，请将其格式化为标准的场景描写，
# 用于指导场景图片生成。
#
# ## 场景原始信息
# {scene_details}
#
# ## 输出要求
# 请以 JSON 数组格式输出每个场景的标准描写：
#
# ```json
# [
#   {{
#     "name": "场景名",
#     "type": "室内/室外",
#     "summary": "3-5个氛围关键词，如：烟火气、怀旧、温暖",
#     "description": "场景的完整格式化描写，包含环境描述、色彩基调、光线氛围、关键道具等，适合用于图片生成的详细描述文本",
#     "image_prompt": "中文逗号分隔的视觉关键词，如：老街，青砖灰瓦老房子，屋檐下褪色红灯笼，石板路，清晨薄雾"
#   }}
# ]
# ```
#
# ## 注意事项
# - description 和 image_prompt 必须基于上方的原始信息，不要编造原文没有的细节
# - 如果原始信息描写不足，合理补充并标注[补充]标记
# - 场景之间要有视觉差异化
# - 只输出 JSON 数组，不要输出任何其他内容
# """
#
# PROP_GEN_PROMPT = """你是一个短剧道具设计师。以下是从小说中提取的道具原始信息，请将其格式化为标准的道具描写，
# 用于指导道具图片生成。
#
# ## 道具原始信息
# {prop_details}
#
# ## 输出要求
# 请以 JSON 数组格式输出每个道具的标准描写：
#
# ```json
# [
#   {{
#     "name": "道具名",
#     "importance": "关键/普通",
#     "description": "道具的完整格式化描写，包含外观描述、细节特征、使用场景、象征意义等，适合用于图片生成的详细描述文本",
#     "image_prompt": "中文逗号分隔的视觉关键词，如：老旧黄铜钥匙，氧化发绿，钥匙柄刻有模糊的陈字，磨损的齿痕"
#   }}
# ]
# ```
#
# ## 注意事项
# - description 和 image_prompt 必须基于上方的原始信息，不要编造原文没有的细节
# - 如果原始信息描写不足，合理补充并标注[补充]标记
# - 只输出 JSON 数组，不要输出任何其他内容
# """
#
# EPISODE_OUTLINE_PROMPT = """基于以下短剧全局设定和章节映射，为每集生成剧本大纲。
#
# ## 故事概要与世界观
# {novel_summary}
# {world_setting}
#
# ## 人物关系
# {character_relations}
#
# ## 章节摘要
# {chapter_summaries}
#
# ## 集数映射建议
# {episode_mapping}
#
# ## 输出要求
# 请以 JSON 格式输出每集大纲：
#
# [
#     {{
#         "episode_number": 1,
#         "title": "集标题",
#         "summary": "本集剧情摘要（200-300字）",
#         "key_events": ["关键事件1", "关键事件2"],
#         "key_characters": ["本集关键角色"],
#         "cliffhanger": "本集结尾悬念"
#     }}
# ]
#
# 注意：
# - 每集要有明确的开头、冲突和结尾
# - 集与集之间要有连贯性
# - 每集结尾最好有悬念或转折
# - key_characters 要与提取的角色列表对应
# """
#
# FIRST_EP_STORYBOARD_PROMPT = """基于以下信息，为第一集生成详细的分镜剧本。
#
# ## 故事概要
# {novel_summary}
#
# ## 第一集大纲
# {first_episode_outline}
#
# ## 人物特征（已格式化）
# {character_profiles}
#
# ## 场景描写（已格式化）
# {scene_descriptions}
#
# ## 角色原始细节（用于补充分镜中的角色行为）
# {character_details_raw}
#
# ## 输出要求
# 请以 JSON 格式输出分镜内容：
#
# {{
#     "scenes": [
#         {{
#             "scene_number": 1,
#             "location": "场景名（必须与场景列表中的名称一致）",
#             "time": "白天/夜晚/黄昏",
#             "characters": ["出场角色列表"],
#             "action": "角色动作和画面描述",
#             "dialogue": "角色对白（如有，尽量使用原文对话）",
#             "camera": "镜头运动描述（推/拉/摇/移/跟）",
#             "emotion": "情绪氛围",
#             "duration": 5
#         }}
#     ]
# }}
#
# 注意：
# - 每个分镜时长不超过15秒
# - 动作描写要可视化，适合直接生成视频
# - 角色外貌和行为要与人物特征描述一致
# - 场景描写要与场景列表中的视觉细节一致
# - 对白尽量使用原文中的对话，保留角色语言特色
# - 镜头语言要专业
# - 分镜数量控制在 15-30 个（对应 3-5 分钟视频）
# """
